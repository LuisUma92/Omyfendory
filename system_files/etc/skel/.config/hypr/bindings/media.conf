# OSD on focused monitor
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

# Volume
bindeld = ,XF86AudioRaiseVolume, Volume up, exec, $osdclient --output-volume raise
bindeld = ,XF86AudioLowerVolume, Volume down, exec, $osdclient --output-volume lower
bindeld = ,XF86AudioMute, Mute, exec, $osdclient --output-volume mute-toggle
bindeld = ,XF86AudioMicMute, Mute mic, exec, $osdclient --input-volume mute-toggle

# Brightness
bindeld = ,XF86MonBrightnessUp, Brightness up, exec, brightnessctl set +5%
bindeld = ,XF86MonBrightnessDown, Brightness down, exec, brightnessctl set 5%-

# Precise adjustments with Alt
bindeld = ALT, XF86AudioRaiseVolume, Volume +1, exec, $osdclient --output-volume +1
bindeld = ALT, XF86AudioLowerVolume, Volume -1, exec, $osdclient --output-volume -1
bindeld = ALT, XF86MonBrightnessUp, Brightness +1%, exec, brightnessctl set +1%
bindeld = ALT, XF86MonBrightnessDown, Brightness -1%, exec, brightnessctl set 1%-

# Media keys (playerctl)
bindld = , XF86AudioNext, Next track, exec, playerctl next
bindld = , XF86AudioPause, Pause, exec, playerctl play-pause
bindld = , XF86AudioPlay, Play, exec, playerctl play-pause
bindld = , XF86AudioPrev, Previous track, exec, playerctl previous
